# Restart OC - Sui Object Trading DApp

一个基于Sui区块链的去中心化对象交易DApp，支持机器人托管、多代币交易、定时转账和管理员控制面板。

## 项目结构

```
restartOC/
├── frontweb/          # 前端React应用 - 主要交易界面
│   ├── src/          # 源代码
│   ├── package.json  # 前端依赖
│   └── vite.config.ts # Vite配置
├── manager/           # 管理员控制面板 - 代币管理
│   ├── src/          # 管理面板源码
│   ├── package.json  # 管理面板依赖
│   └── README.md     # 管理面板详细说明
├── contracts/         # Sui Move合约
│   ├── sources/      # 合约源码
│   │   ├── trading_object.move    # 核心交易合约
│   │   ├── object_wallet.move     # 对象钱包合约
│   │   ├── scheduled_transfer.move # 定时转账合约
│   │   └── oc_bot.move            # 机器人合约
│   └── Move.toml     # Move配置
├── agentOC/          # 机器人托管服务
│   ├── src/          # 机器人源码
│   └── package.json  # 机器人依赖
├── package.json      # 根项目配置
└── README.md         # 项目说明
```

## 功能特性

### 🎨 前端交易界面 (frontweb/)
- 🔐 Sui钱包连接和多网络支持
- 📦 对象创建、上架、下架交易
- 💰 多代币支持 (USDC, SUI等)
- 🤖 表情包和头像展示
- 📊 Walrus blob存储集成
- 💳 对象钱包管理
- ⏰ 定时转账功能
- 📈 实时余额显示

### 🛠️ 管理员控制面板 (manager/)
- 🔐 管理员权限验证
- 📊 查看当前支持的代币类型
- ➕ 添加新的代币类型
- ❌ 删除现有的代币类型
- 🌐 多网络支持 (测试网/开发网/主网)
- 🎨 现代化 UI 设计

### 📝 智能合约 (contracts/)
- **TradingObject**: 核心交易对象管理
- **ObjectWallet**: 对象关联钱包系统
- **ScheduledTransfer**: 定时转账功能
- **OCBot**: 机器人托管合约
- 🔒 共享对象管理
- 💸 安全转账机制
- 🎯 多代币市场逻辑
- 🔄 销毁机制

### 🤖 机器人服务 (agentOC/)
- 🤖 自动对象托管
- ⚡ 自动转账处理
- 📈 交易监控
- 🔔 事件通知
- 🔄 自动交易策略
- 📊 风险管理

## 快速开始

### 1. 环境要求

- Node.js >= 18.0.0
- npm >= 9.0.0
- Sui CLI
- Git

### 2. 一键安装和设置

```bash
# 克隆项目
git clone <repository-url>
cd restartOC

# 安装所有子项目依赖
npm run install:all
```

### 3. 配置环境变量

#### 前端配置 (frontweb/)
```bash
cd frontweb
cp .env.example .env
# 编辑 .env 文件，填入相应的配置
```

#### 管理面板配置 (manager/)
```bash
cd manager
cp .env.example .env
# 编辑 .env 文件，填入相应的配置
```

#### 机器人服务配置 (agentOC/)
```bash
cd agentOC
cp .env.example .env
# 编辑 .env 文件，填入相应的配置
```

### 4. 部署合约

```bash
cd contracts
sui move build
sui client publish --gas-budget 1000000000
```

将部署后的合约地址更新到各个环境变量中。

### 5. 启动应用

```bash
# 启动前端交易界面
cd frontweb ; npm run dev

# 启动管理员控制面板
cd manager ; npm run dev

# 启动机器人服务
cd agentOC ; npm start

# 编译合约
cd contracts ; sui move build
```

## 开发脚本

```bash
# 安装所有依赖
npm run install:all

# 构建所有项目
npm run build

# 运行测试
npm run test

# 代码检查
npm run lint

# 清理所有构建文件
npm run clean

# 合约相关
npm run contract:build  # 构建合约
npm run contract:test   # 测试合约
npm run contract:publish # 发布合约
```

## 技术栈

- **前端**: React 18, TypeScript, Vite, Tailwind CSS
- **管理面板**: React 18, TypeScript, Vite, Tailwind CSS
- **区块链**: Sui, Move
- **钱包**: @mysten/wallet-kit
- **存储**: Walrus
- **机器人**: Node.js, TypeScript
- **构建工具**: Concurrently

## 核心功能详解

### 🏷️ 对象结构

每个交易对象包含：
- `owner`: 拥有者地址
- `bot`: 机器人字段
- `emoji`: 表情包
- `avatar`: 头像
- `blobId`: Walrus存储ID
- `price`: 代币价格
- `wallet_id`: 关联钱包ID (新增)

### 💰 多代币支持

- 支持多种代币类型 (USDC, SUI等)
- 动态代币类型管理
- 市场管理员可添加/移除支持的代币
- 类型安全的代币操作

### 💳 对象钱包系统

- 每个交易对象可关联独立钱包
- 支持多代币余额存储
- 安全的存取款操作
- 钱包销毁机制

### ⏰ 定时转账

- 创建定时转账任务
- 自动执行到期转账
- 转账取消功能
- 市场验证机制

## 环境变量说明

### 前端环境变量 (frontweb/.env)
```env
# Sui Network Configuration
VITE_SUI_NETWORK=testnet

# Contract Configuration
VITE_PACKAGE_ID=0x你的包ID
VITE_MARKETPLACE_ID=0x你的市场ID
VITE_USDC_TYPE=0x你的USDC类型

# Walrus Configuration
VITE_WALRUS_ENDPOINT=你的Walrus端点
```

### 管理面板环境变量 (manager/.env)
```env
# Sui Network Configuration
VITE_SUI_NETWORK=testnet

# Contract Configuration - Testnet
VITE_TESTNET_PACKAGE_ID=0x你的测试网包ID
VITE_TESTNET_MARKETPLACE_ID=0x你的测试网市场ID
VITE_TESTNET_ADMIN_ADDRESS=0x你的测试网管理员地址

# Contract Configuration - Mainnet
VITE_MAINNET_PACKAGE_ID=0x你的主网包ID
VITE_MAINNET_MARKETPLACE_ID=0x你的主网市场ID
VITE_MAINNET_ADMIN_ADDRESS=0x你的主网管理员地址
```

### 机器人环境变量 (agentOC/.env)
```env
AGENT_PRIVATE_KEY=机器人私钥
CONTRACT_PACKAGE_ID=合约包ID
ENABLE_AUTO_TRADING=true
MAX_TRADE_AMOUNT_SUI=1000
MIN_PROFIT_THRESHOLD=0.1
```

## 项目架构

### 前端架构
```
frontweb/src/
├── components/
│   ├── WalletInfo.tsx      # 钱包信息组件
│   ├── ObjectMarketplace.tsx # 市场主组件
│   ├── ObjectForm.tsx      # 对象创建表单
│   ├── ObjectWallet.tsx    # 对象钱包组件
│   └── ObjectList.tsx      # 对象列表组件
├── services/
│   ├── walletService.ts    # 钱包服务
│   ├── contractService.ts  # 合约服务
│   └── marketplaceService.ts # 市场服务
├── App.tsx                 # 主应用组件
└── main.tsx               # 应用入口
```

### 管理面板架构
```
manager/src/
├── components/
│   ├── TokenManager.tsx    # 代币管理主组件
│   ├── WalletConnect.tsx   # 钱包连接组件
│   └── NetworkSelector.tsx # 网络选择器
├── services/
│   └── tokenManagerService.ts # 代币管理服务
├── config/
│   ├── contractConfig.ts   # 合约配置
│   └── networkConfig.ts    # 网络配置
└── App.tsx                 # 主应用组件
```

### 合约架构
```
contracts/sources/
├── trading_object.move     # 核心交易合约
├── object_wallet.move      # 对象钱包合约
├── scheduled_transfer.move # 定时转账合约
└── oc_bot.move            # 机器人合约
```

### 机器人架构
```
agentOC/src/
├── services/
│   ├── objectMonitor.ts   # 对象监控服务
│   ├── autoTrader.ts      # 自动交易服务
│   └── transferManager.ts # 转账管理服务
├── utils/
│   └── logger.ts          # 日志工具
└── index.ts               # 机器人主入口
```

## 开发指南

### 添加新功能
1. **前端**: 在 `frontweb/src/components/` 中添加新组件
2. **管理面板**: 在 `manager/src/components/` 中添加新组件
3. **合约**: 在 `contracts/sources/` 中添加新合约
4. **机器人**: 在 `agentOC/src/services/` 中添加新服务

### 测试
```bash
# 前端测试
cd frontweb ; npm test

# 管理面板测试
cd manager ; npm test

# 合约测试
cd contracts ; sui move test

# 机器人测试
cd agentOC ; npm test
```

### 部署
1. 更新环境变量
2. 构建项目：`npm run build`
3. 部署合约：`npm run contract:publish`
4. 启动服务：`npm run start`

## 安全注意事项

- 🔒 私钥安全：不要在生产环境中硬编码私钥
- 🛡️ 输入验证：所有用户输入都需要验证
- 💰 资金安全：使用多重签名和时间锁
- 📝 审计：定期进行代码审计
- 🚨 权限控制：严格区分用户和管理员权限

## 贡献指南

1. Fork 项目
2. 创建功能分支：`git checkout -b feature/new-feature`
3. 提交更改：`git commit -am 'Add new feature'`
4. 推送分支：`git push origin feature/new-feature`
5. 提交Pull Request

## 许可证

MIT License

## 支持

- 📧 邮箱：support@restartoc.com
- 💬 Discord：[加入社区](https://discord.gg/restartoc)
- 📖 文档：[查看文档](https://docs.restartoc.com)


## 更新日志

### v2.0.0 (2025-11-21)
- ✨ **新增管理员控制面板** - 独立的代币管理界面
- 💳 **对象钱包系统** - 每个对象可关联独立钱包
- ⏰ **定时转账功能** - 支持定时自动转账
- 🏷️ **多代币支持** - 动态代币类型管理
- 🔄 **销毁机制** - 对象和钱包的安全销毁

### v1.0.0
- ✨ 初始版本发布
- 🎨 基础前端界面
- 📝 核心交易合约
- 🤖 机器人托管服务
- 📊 Walrus存储集成

### 最新修复 (2025-11-18)
- ⏰ **定时转账** - 修复数量显示和错误提示
- 💰 **余额显示** - 修复负数显示和精度问题
- 🛒 **购买功能** - 修复代币类型匹配和交易失败
- 🎨 **UI改进** - 优化用户界面和交互体验

---

*最后更新：2025-11-21*
